// Test: Sequential multi blocks
function addOne(n) do
    return n + 1
end

multi r1 do
    thread addOne(10) -> a
end

PRINT(r1.a.value)

multi r2 do
    thread addOne(r1.a.value) -> b
end

PRINT(r2.b.value)

multi r3 do
    thread addOne(r2.b.value) -> c
end

PRINT(r3.c.value)
