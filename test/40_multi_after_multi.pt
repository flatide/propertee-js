// Test: Sequential multi blocks
function addOne(n) do
    return n + 1
end

multi
    thread addOne(10) -> a
end

PRINT(a.value)

multi
    thread addOne(a.value) -> b
end

PRINT(b.value)

multi
    thread addOne(b.value) -> c
end

PRINT(c.value)
