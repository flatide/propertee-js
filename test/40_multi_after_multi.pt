// Test: Sequential multi blocks
thread addOne(n) do
    return n + 1
end

multi
    addOne(10) -> a
end

PRINT(a.value)

multi
    addOne(a.value) -> b
end

PRINT(b.value)

multi
    addOne(b.value) -> c
end

PRINT(c.value)
