// Test: Functions with loops in multi blocks
function accumulate(n) do
    total = 0
    i = 1
    loop i <= n infinite do
        total = total + i
        i = i + 1
    end
    return total
end

function buildList(n) do
    result = []
    i = 0
    loop i < n infinite do
        result = PUSH(result, i * 10)
        i = i + 1
    end
    return result
end

multi
    thread accumulate(10) -> sum
    thread buildList(4) -> list
end

PRINT(sum.value)
PRINT(list.value)
