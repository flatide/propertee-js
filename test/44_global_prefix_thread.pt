// Test: :: read in function inside multi block (reads from global snapshot)
x = 100
y = "hello"

function readGlobals() do
    return TO_STRING(::x) + " " + ::y
end

multi res do
    thread readGlobals() -> r
end

PRINT(res.r.value)
